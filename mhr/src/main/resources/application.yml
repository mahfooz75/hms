server:
  port: 8083
spring:
  main:
    banner-mode: off
  application:
    name: patient-mhr
  data:
    mongodb:
      uri: mongodb://localhost:27017/hms-mhr
      auto-index-creation: true
logging:
  level:
    org.mongodb.driver.protocol.command: DEBUG # THIS WILL SHOW QUERY IN LOG